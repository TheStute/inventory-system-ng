nav.navbar.navbar-expand-md.navbar-dark.bg-primary.fixed-top
  a.navbar-brand(routerLink="/") Stute Inventory
  button.navbar-toggler.hidden-sm-up((click)="this.isCollapsed = !this.isCollapsed",
      type="button",
      aria-controls="navbarsDefault",
      aria-expanded="false",
      aria-label="Toggle Navigation")
    span.navbar-toggler-icon
  .collapse.navbar-collapse#navbarsDefault([ngbCollapse]="isCollapsed")
    ul.navbar-nav
      li.nav-item
        a.btn.btn-primary(routerLink="scan") Scanner
      li.nav-item
        a.btn.btn-primary(routerLink="generate") Generate PDF
